{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "types": ["node"],
    "baseUrl": ".."
  },
  "include": ["**/*.ts", "**/*.mts"],
  "exclude": ["../node_modules", "../dist"]
}
